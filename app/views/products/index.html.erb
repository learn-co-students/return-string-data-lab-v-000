<% @products.each do |product| %>
  <%= render partial: "products", locals: {product: product} %>
<% end %>
 
<script type="text/javascript" charset="utf-8">
$(function () {
  $(".js-more").on('click', function() {
    let id = $(this).data("id");
    $.get("/products/" + id + "/description", function(data) {
      $("#description-" + id).text(data);
    $.get("/products/" + id + "/inventory", function(data) {

      gets inventory amount
      shows message depending on inventory amount
      where is it showing the message?
        put it in a div 
        

      let productAvailability = "<strong>Available</strong>"
     
     if inventory < 0, 
     productAvailability = "<strong>Available</strong>"
     else inventory
     
    
      $("#inventory-" + id).text(data);


    });
  });
});
</script>
