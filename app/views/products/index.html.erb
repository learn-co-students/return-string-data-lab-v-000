<%= render(@products) || "There are no products available." %>

<script>
  $(function(){
    $(".js-more").on("click", function(){
        console.log("In script jQuery thing")
        var id = this.id
        $.get("/products/" + id + "/body", ourId=id, function(response){
          $("#" + ourId + "-details")[0].innerHTML = response
        })
        $.get("/products/" + id + "/inventory_available", function(response){
          if (response == "true"){
            $("#" + ourId + "-details")[0].innerHTML += "<br>" + "Available"
          } else {
            $("#" + ourId + "-details")[0].innerHTML += "<br>" + "Sold Out"}
        })

    }) 
  })
</script>
